# Data

This folder is intended for datasets, extracted DOM features, and preprocessing outputs used by the machine learning pipeline.

Large datasets should not be committed directly unless required for the project submission.
