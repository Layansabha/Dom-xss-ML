# Source Code

This folder is intended for the main application source code.

Suggested structure:

- `backend/` for Flask API and pipeline logic
- `frontend/` for React interface
- `pipeline/` for crawling, DOM extraction, feature extraction, and prediction modules
